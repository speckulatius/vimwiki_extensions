image: registry.gitlab.com/specktrum/vimwiki_extensions:latest

stages:
  - lint
  - typing
  - test

lint:
  stage: lint
  script:
    - ./run.sh lint

typing:
  stage: typing
  script:
    - ./run.sh typing

test:
  stage: test
  script:
    - ./run.sh test
